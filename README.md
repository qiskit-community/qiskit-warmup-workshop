# qiskit-warmup-workshop
A workshop on Qiskit for the Amravati QIC

Please clone this repository locally using `git clone` and then keep updating it locally by using `git pull` to get the latest changes.
